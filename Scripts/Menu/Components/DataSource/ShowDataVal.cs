using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class ShowDataVal : MonoBehaviour
{
    /*
    Text textfield;
    private TMPro.TextMeshProUGUI txtmesh;
    public DatabaseSource source;
    public string tableName;
    public string valueName;

    // Start is called before the first frame update
    void Start()
    {
        textfield = GetComponent<Text>();
        txtmesh = gameObject.GetComponent<TMPro.TextMeshProUGUI>();
        //InvokeRepeating("updateField", updateTime, updateTime);
    }

    // Update is called once per frame
    public void updateField()
    {
        if (textfield != null)
        {
            textfield.text = source.getTable(tableName).get;
        }
        if (txtmesh != null)
        {
            txtmesh.text = GameManager.ins.getUIVal(valueToDisplay);
        }

    }

    private void OnEnable()
    {
        updateField();
    }

    private void Awake()
    {
        updateField();
    }*/
}
